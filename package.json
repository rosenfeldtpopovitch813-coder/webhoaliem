{
  "name": "webhoaliem",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node tests/check.cjs",
    "start": "node server.cjs",
    "vercel-build": "npm run build:css",
    "build:css": "tailwindcss -i css/utilities.input.css -o css/utilities.css --minify",
    "test:browser": "node tests/browser.cjs"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
